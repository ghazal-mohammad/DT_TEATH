// ════════════════════════════════════════════════════════════════════════════
// mock_user_data.dart
//
// بيانات وهمية للمستخدم — مركزية في ملف واحد بدل تكرارها في كل صفحة.
//
// 🎯 الهدف:
//   في غياب Backend + AuthCubit حقيقي، نحتاج placeholder للاسم والدور
//   نعرضه في السايدبار (switcher) والتوب بار. هذه البيانات مؤقّتة.
//
// 🔮 خطّة الانتقال:
//   عند جاهزية الـ Backend (Phase 6+):
//     1. نضيف AuthCubit يقرأ JWT.
//     2. نُمرّر بيانات المستخدم الفعلي للـ AppShellLayout عبر BlocBuilder.
//     3. نحذف هذا الملف بالكامل — لا توجد قيم hardcoded في الإنتاج.
//
// ملاحظة convention: النصوص هنا ليست مرشّحة للترجمة لأنها أسماء بشر — لذلك
// لا تنتمي للـ ARB. الدور يأتي من ARB دائماً (context.l10n.role*).
// ════════════════════════════════════════════════════════════════════════════

/// بيانات المستخدم الوهمية المستخدمة في Phase 4 و 5 قبل ربط Auth الحقيقي.
class MockUserData {
  MockUserData._();

  /// اسم المستخدم الافتراضي لعرض في السايدبار (نظام المستودع).
  /// (سيُستبدل من JWT.name في Phase 6+).
  static const String defaultUserName = 'أحمد محمود';

  /// اسم المستخدم الوهمي لنظام المخبر.
  static const String labUserName = 'رامي الصالح';

  /// رقم تعريفي وهمي لمدير المستودع.
  static const String defaultWarehouseUserId = 'WH-001';

  /// رقم تعريفي وهمي لمدير المخبر.
  static const String defaultLabUserId = 'LAB-001';
}
